// breadcrumb.src — path trail with "/" separators; the last item is the
// current page (ink, not a link). `hrefs` pairs with `labels` ("" for no link).

func ui_breadcrumb(labels, hrefs) (h) {
    h = "<nav class=\"flex items-center gap-2 text-sm text-stone-500 dark:text-stone-400\">"
    i := 0
    for _, l := range labels {
        if i > 0 { h = h + "<span class=\"text-stone-300\">/</span>" }
        if i == len(labels) - 1 {
            h = h + "<span class=\"font-medium text-stone-900 dark:text-stone-100\">" + ui_esc(l) + "</span>"
        } else if len(hrefs[i]) > 0 {
            h = h + "<a class=\"hover:text-stone-900 dark:hover:text-stone-100 hover:underline underline-offset-4\" href=\"" + hrefs[i] + "\">" + ui_esc(l) + "</a>"
        } else {
            h = h + "<span>" + ui_esc(l) + "</span>"
        }
        i = i + 1
    }
    h = h + "</nav>"
}
