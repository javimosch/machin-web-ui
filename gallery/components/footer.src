// footer.src — quiet page footer above a hairline: left text, right link row.
// `labels`/`hrefs` pair up for the links.

func ui_footer(left, labels, hrefs) (h) {
    h = "<footer class=\"mt-10 flex flex-wrap items-center justify-between gap-2 border-t border-stone-200 pt-5 text-xs text-stone-500\">"
    h = h + "<span>" + left + "</span>"
    h = h + "<nav class=\"flex items-center gap-4\">"
    i := 0
    for _, l := range labels {
        h = h + "<a class=\"transition hover:text-stone-900\" href=\"" + hrefs[i] + "\">" + ui_esc(l) + "</a>"
        i = i + 1
    }
    h = h + "</nav></footer>"
}
